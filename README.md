# 2026 财运巅峰预测 - 春节版 MVP

基于八字命理的 2026 年财运预测应用，结合 Gemini AI 进行智能分析。

## 功能特性

- 🎯 **财运预测**：输入出生时间（精确到分钟），获取个人化的 2026 年财运报告
- 📊 **财运排行榜**：查看所有用户的财运排名
- 💑 **财运合盘**：与好友进行财运契合度分析
- 🎨 **海报生成**：生成精美的分享海报，内嵌二维码
- 🔗 **分享功能**：支持下载海报和复制链接分享

## 技术栈

- **前端**：Next.js 14 + React 18 + TailwindCSS
- **后端**：Next.js API Routes
- **数据库**：SQLite (better-sqlite3)
- **AI**：Google Gemini API
- **二维码**：qrcode.react
- **海报下载**：html2canvas

## 快速开始

### 1. 安装依赖

```bash
cd fortune-2026
npm install
```

### 2. 配置环境变量

复制 `.env.example` 为 `.env.local`，并填写你的 API Key：

```bash
cp .env.example .env.local
```

编辑 `.env.local`：

```env
# Google Gemini API Key (必填)
GEMINI_API_KEY=your_gemini_api_key_here

# 网站URL (用于生成二维码)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

### 3. 获取 Gemini API Key

1. 访问 [Google AI Studio](https://aistudio.google.com/)
2. 创建 API Key
3. 将 Key 填入 `.env.local`

### 4. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000 即可使用。

## 项目结构

```
fortune-2026/
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   ├── fortune/          # 财运报告 API
│   │   │   ├── leaderboard/      # 排行榜 API
│   │   │   └── compatibility/    # 合盘 API
│   │   ├── result/[id]/          # 报告结果页
│   │   ├── leaderboard/          # 排行榜页面
│   │   ├── compatibility/        # 合盘页面
│   │   │   └── result/           # 合盘结果页
│   │   ├── layout.tsx
│   │   ├── page.tsx              # 首页
│   │   └── globals.css
│   └── lib/
│       ├── db.ts                 # 数据库操作
│       ├── gemini.ts             # Gemini API 集成
│       └── types.ts              # TypeScript 类型定义
├── fortune.db                    # SQLite 数据库（自动创建）
├── package.json
└── README.md
```

## API 说明

### POST /api/fortune
创建财运报告

请求体：
```json
{
  "nickname": "用户昵称",
  "birthYear": 1990,
  "birthMonth": 6,
  "birthDay": 15,
  "birthHour": 14,
  "birthMinute": 30
}
```

### GET /api/fortune/[id]
获取财运报告详情

### GET /api/leaderboard
获取财运排行榜

### POST /api/compatibility
生成财运合盘报告

## 部署

### Vercel 部署

1. Fork 本项目到你的 GitHub
2. 在 Vercel 中导入项目
3. 配置环境变量 `GEMINI_API_KEY` 和 `NEXT_PUBLIC_SITE_URL`
4. 部署

注意：由于使用了 SQLite，Vercel 部署时数据库是临时的。生产环境建议使用 PostgreSQL 或 MySQL。

## 待完成功能

- [ ] Nano Banana 海报生成集成（等待提示词）
- [ ] 币安广场分享功能（API 对接）
- [ ] 更精确的八字排盘算法（农历转换）
- [ ] 用户登录/注册系统

## 许可证

MIT License
